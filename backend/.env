//El archivo  .env no se sube a GIt por que puede contener información sensible como contraseñas y credenciales de acceso a la base de datos.
// en el GIT ignore deben estar los archivos .env y node_modules/
DB_NAME = DWEBII
DB_USER = sa
DB_PASSWORD = danny2001
DB_HOST = localhost
DB_PORT = 1433
PORT = 3000